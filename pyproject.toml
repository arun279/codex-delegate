[tool.ruff]
target-version = "py39"
line-length = 150

[tool.ruff.lint]
external = ["S"]

[tool.mypy]
python_version = "3.9"
strict = true
pretty = true
show_error_codes = true

[tool.vulture]
min_confidence = 80
paths = ["hooks", "scripts", "tests"]
